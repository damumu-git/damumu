# DAMUMU 最小可发布 MVP

目标：完成“用户注册 → 找附近活动 → 发起活动 → 报名 → 发起人审核 → 申请人收到通知”的真实业务闭环，并具备最小运营后台。

## 1. 用户与个人资料

### 用户流程

- 邮箱注册和登录。
- Debug 环境保留固定测试账号与自动登录；Release 构建禁止自动登录。
- 注册必填：昵称、邮箱、密码。
- 注册选填：系统头像或自定义头像、联系方式。
- 个人中心可修改昵称、头像和联系方式。

### 联系方式

- MVP 支持手机号、KakaoTalk ID、微信号三种类型。
- 联系方式默认仅本人可见；活动报名时由申请人明确选择是否提供给发起人。
- API 返回时对无权访问者隐藏联系方式。

### 头像

- 系统提供约 20 个统一风格的 512×512 JPEG 头像。
- 用户可选择系统头像，也可从相册或相机上传。
- 上传流程：选择图片 → 锁定 1:1 裁剪 → 输出 512×512 JPEG → 最大 1 MB → 上传。
- Web、Android、iOS 均支持；Web 使用专用裁剪界面和中文操作文案。
- 服务端再次校验 MIME、JPEG 文件签名、尺寸和体积。

### 验收

- 新用户不上传照片也能从系统头像完成注册。
- 上传 PNG/HEIC 等来源图片后，服务端最终收到合规 JPEG。
- 页面刷新、App 重启后登录态和头像保持不变。

## 2. 附近活动

### 规则

- App 获得用户前台定位权限后提交纬度、经度和搜索半径。
- 后端使用 PostGIS 计算距离，默认按距离由近到远排序。
- 默认半径 20 km，可选 5/10/20/50 km。
- 活动卡片展示：标题、小分类、开始时间、区域、距离、费用、已通过人数/人数上限。
- 未授权定位时回退到用户资料中的城市/行政区，并允许手动选择。
- 首页不再使用写死的演示距离。

### 验收

- 用户移动位置或刷新定位后，列表顺序会发生实际变化。
- 已取消、已隐藏、已删除和非公开活动不出现在列表。

## 3. 创建活动

### 必填字段

- 活动标题。
- 大分类和小分类（活动最终绑定小分类）。
- 开始时间和结束时间。
- 最少人数和最多人数。
- 人均最低费用和最高费用；免费活动均为 0。
- 币种，首发默认 KRW。
- 活动地点名称、公开区域、纬度和经度。
- 公告信息：所有浏览者可见的活动说明。

### 选填字段

- 详细地址：仅报名通过者可见。
- 备注信息：参与要求、携带物品、费用说明等。
- 是否需要发起人审核；MVP 默认需要审核。

### 状态

`draft → published → full/completed/cancelled/hidden`

MVP 不要求管理员审核后才能发布。预留 `review_status`：

- `not_required`：当前默认。
- `pending`：未来启用审核时使用。
- `approved`。
- `rejected`。

同时预留管理员提交审核决定的接口，不在当前 App 发布流程中强制调用。

## 4. 活动报名

### 申请字段

- 申请人：由登录态确定，不能由客户端伪造。
- 报名人数：最少 1 人，不能超过活动剩余名额或活动设置的单次上限。
- 备注信息：自我介绍、同行人员说明等。
- 是否向发起人共享联系方式。

### 状态

`applied → approved/rejected/withdrawn`

- 同一用户对同一活动只有一条有效申请。
- 发起人不能报名自己的活动。
- 申请人数必须计入容量判断；不能只按申请记录条数计算。

## 5. 发起人审核申请

- 仅活动发起人可查看申请列表。
- 列表展示申请人昵称、头像、申请人数、信誉摘要、申请备注和申请时间。
- 通过时再次原子检查剩余名额，防止并发超员。
- 拒绝时拒绝理由必填，长度 2–300 字。
- 审核操作记录审核人、时间和理由。
- 已处理申请不能被重复处理；必要时另设“撤销通过”流程，不复用拒绝接口。

## 6. 通知闭环

审核成功后，在同一个数据库事务中写通知：

- 通过：`event_application_approved`。
- 拒绝：`event_application_rejected`，正文包含拒绝理由。

通知列表展示标题、正文、活动信息、创建时间和已读状态。点击通知进入对应活动详情。MVP 先做站内通知；推送通知作为后续版本。

## 7. Admin 最小后台

### 类目管理

- 采用两级结构：大分类 → 小分类。
- 大小分类均支持新增、查看、编辑、启用/停用和删除。
- 被活动引用的分类禁止物理删除，只能停用；无引用分类可删除。
- 字段：代码、中文/英文/韩文名称、说明、图标、主题色、排序、启用状态。
- 每个分类必须有 icon。MVP 可使用统一设计的图标资产；运营后台支持替换。

### 用户管理

- 用户列表、搜索、详情。
- 查看状态、基本资料、创建/参加活动数量和举报数量。
- 暂停、恢复、封禁账户，并记录原因和审计日志。

### 活动管理

- 活动列表、搜索和状态筛选。
- 查看活动详情、发起人和报名情况。
- 隐藏、恢复、取消活动，并填写处置原因。
- 预留活动审核接口：提交审核、通过、拒绝；当前不阻塞活动发布。

## 8. 明确不进入 MVP

- Apple/Google/手机号登录。
- 支付、退款和平台抽成。
- 实时聊天推送和手机系统推送。
- 推荐算法；MVP 只使用距离、时间和基础筛选。
- 活动自动风控、AI 审核和实名认证。
- 多城市运营规则和复杂 RBAC。

## 9. 实施顺序

1. 数据迁移：联系方式、活动人数/费用/公告/备注、报名人数/拒绝理由、活动审核预留字段。
2. API：用户资料、活动创建/附近列表、报名审核事务与通知。
3. App：真实 API 数据替换演示数据，完成六个核心流程。
4. 头像：补足约 20 个资产，验证三端上传。
5. Admin：两级类目 CRUD、用户管理、活动管理和审核预留操作。
6. 自动化测试：API 集成测试、Flutter 核心组件测试、Admin 构建检查。
7. Google Play 内部测试：隐私政策、数据安全表、测试账号和 AAB。

## 10. MVP 发布判定

以下全部通过才算 MVP 完成：

- 空数据库可按迁移顺序初始化。
- 两个真实测试账号能完成发布与报名审核闭环。
- 首页活动来自 API，并按真实距离排序。
- 拒绝理由能在申请人通知中看到。
- 头像选择和上传在 Web、Android 真机至少各验证一次。
- Admin 能维护两级类目、用户和活动。
- Release 包不自动登录、不包含开发密钥，API 使用 HTTPS。
