# DAMUMU 首发功能与全链路 Flow

> 文档目标：定义 DAMUMU 首次公开发布所需的产品闭环，并明确 App、REST API、Admin 三端职责、交叉依赖、状态、权限、异常和验收标准。
>
> 本文定义计划中的首发范围，并非当前实现状态；当前事实见 `docs/STATUS.md`，长期已采用决策见 `docs/DECISIONS.md`，实际代码和验证结果优先。`MVP.md` 保留为早期方案参考。

## 0. 首发目标与边界

首发必须让真实用户安全地完成下面的完整闭环：

```text
注册/登录
→ 完善资料
→ 自动定位或手选区域
→ 搜索和筛选活动
→ 查看公开活动信息
→ 报名
→ 组织者审核
→ 获批后查看详细集合地点
→ 活动沟通与签到
→ 活动结束
→ 双向评价 / 举报
→ Admin 可处理异常
```

首发原则：

- 先保证闭环、权限、隐私、风控和可运营，再扩展推荐算法。
- App 不直接信任客户端传来的用户 ID、评分、区域名称或权限状态。
- 所有关键限制必须由 API 执行；App 只负责提前提示和友好展示。
- Admin 的每个处置操作必须有操作人、原因、时间和审计记录。
- 详细地点、联系方式、安全位置属于敏感信息，不进入公开活动接口。

### 0.1 优先级

- `P0`：缺失则不能公开发布。
- `P1`：建议首发具备，可在功能开关下灰度。
- `P2`：首发后迭代。

### 0.2 实现状态标记

- `已有`：代码中已有主要实现，仍需测试和生产加固。
- `部分`：已有页面、接口或数据表，但闭环不完整。
- `缺失`：需要新增。
- `需整改`：现有实现与首发规则冲突。

## 1. 三端职责总览

| 领域 | App | API | Admin | 交叉点 |
|---|---|---|---|---|
| 账号 | 注册、登录、注销入口 | 身份认证、Token、注销 | 查询、暂停、封禁 | 账号状态即时影响 App 会话 |
| 用户资料 | 编辑资料、头像、兴趣、语言 | 校验、存储、隐私裁剪 | 查看必要资料、治理 | 多语言目录和头像由 Admin 维护 |
| 区域定位 | GPS、手动选择、展示本地化名称 | 坐标解析、区域关联、查询 | 同步和维护区域主数据 | 全部使用同一 `region_code` |
| 活动发现 | 搜索、筛选、排序、列表 | 权限安全的组合查询 | 隐藏违规活动 | 公开结果不得返回私密地点 |
| 活动发布 | 表单、地点搜索、草稿、发布 | 校验、冲突检测、持久化 | 审核/隐藏/取消 | 分类、标签、区域均来自主数据 |
| 报名审核 | 申请、退出、组织者审批 | 容量、限次、冲突、事务 | 处理申诉和异常 | 审批成功后才开放详细地点 |
| 消息通知 | 会话、消息、站内通知 | 成员鉴权、消息存储、通知 | 举报后查看必要证据 | 被屏蔽/封禁后限制通信 |
| 评价信用 | 提交评价、展示聚合分 | 资格校验、聚合计算 | 处理恶意评价 | 筛选只能使用可信聚合值 |
| 举报安全 | 举报、屏蔽、安全会面 | 证据、状态、敏感数据生命周期 | 举报队列、安全警报 | 处置结果反馈用户并留审计 |
| 运营配置 | 读取配置 | 返回公开配置 | 分类、标签、开关、公告 | 配置变更不要求重新发版 |

## 2. 跨端基础能力

### 2.1 正式认证与会话（P0，部分/需整改）

#### App Flow

```text
打开 App
→ 读取安全存储中的 access/refresh token
→ access token 有效：进入首页
→ access token 过期：静默刷新
→ 刷新失败：回到登录页
→ 登录成功：拉取本人资料、公开配置、目录版本
```

#### API 职责

- 使用短时 access token 和可轮换 refresh token。
- 密码使用可靠哈希；登录、注册、刷新和找回密码限流。
- 所有用户接口从 Token 获取用户 ID，不接受客户端伪造 `X-User-Id`。
- `suspended` 用户限制发布、报名和发消息；`banned/deleted` 用户拒绝会话。
- Release 环境禁止 `dev-login` 和自动测试账号登录。
- 记录登录时间、失败次数、Token 撤销和安全事件。

#### Admin 职责

- 管理员使用独立登录、短时 HttpOnly 会话 Cookie、RBAC，建议启用二次验证。
- 禁止把固定 `X-Admin-Key` 编译进前端静态资源。
- 支持暂停、恢复、封禁用户，原因必填并写审计日志。

#### 异常

- 网络失败不清空仍可恢复的本地会话。
- Token 被撤销、账号封禁或删除时立即退出。
- 重复邮箱、弱密码、暴力尝试返回稳定错误码和本地化文案。

### 2.2 多语言（P0，部分）

#### 范围

- App 固定文案：中文、韩文、英文完整覆盖。
- API 错误：返回稳定 `error.code`；App 根据代码本地化，API 文案仅作兜底。
- 系统目录：分类、兴趣、标签、区域、系统头像保存多语言名称。
- 用户内容：保留原文和原文语言；首发不要求自动翻译，可显示原文。
- 地点专有名：保留韩文原名，可附英文/中文名称，不强制机器翻译。

#### Flow

```text
用户切换语言
→ App 立即切换固定文案
→ 保存 locale
→ 重新请求目录/区域显示名
→ 清除按语言缓存的地名标签
→ 用户原始内容不被改写
```

#### Admin 职责

- 分类、兴趣、活动属性标签、系统头像都必须填写中/英/韩名称后才能启用。
- 显示缺失翻译、重复代码和停用项。

### 2.3 目录与公开配置（P0，部分）

目录包括：

- 活动分类：大类 → 中类 → 可发布小类。
- 兴趣：用于个人偏好和活动主题。
- 活动属性标签：如新手友好、室内、提供装备。
- 语言目录、举报原因、取消原因。
- 区域主数据。
- 系统头像。

API 提供目录版本号或 `updated_at`；App 本地缓存，版本变化时刷新。Admin 负责新增、排序、翻译、启停；被业务引用的目录项不得物理删除。

## 3. 用户与个人资料

### 3.1 注册（P0，部分）

#### App Flow

```text
输入邮箱、密码、昵称
→ 阅读并同意服务条款与隐私政策
→ 选择系统头像或上传头像（可稍后）
→ 提交注册
→ 成功后进入资料引导
```

#### API

- 邮箱规范化并保证唯一。
- 校验昵称、密码强度、头像、协议版本和同意时间。
- 创建用户、资料、默认偏好和会话。
- 防止批量注册、撞库和重复请求。

#### Admin

- 可查询注册时间、状态、公开资料和风险摘要。
- 不展示密码、Token 或不必要的敏感资料。

### 3.2 资料与兴趣（P0，部分）

#### App Flow

```text
我的
→ 编辑昵称、简介、头像、语言、常驻区域、兴趣
→ 预览公开效果
→ 保存
```

#### 规则

- 必填：昵称、App 语言；其他字段按产品确认。
- 性别为自愿字段，包含“不愿透露”；不得由头像或姓名推断。
- 联系方式默认私密，只在用户明确授权的流程中披露。
- 自定义头像经过裁剪、压缩和服务端复验。

### 3.3 账号注销（P0，部分）

#### App Flow

```text
设置 → 注销账号
→ 展示影响和不可逆内容
→ 再次认证/确认
→ 若有未来进行中的组织活动，要求先取消或移交
→ 提交注销
→ 清除本地会话
```

#### API / Admin

- 撤销 Token，停止公开展示，按隐私政策匿名化或延迟删除数据。
- 保留依法或安全治理必须保留的数据，并设置到期清理时间。
- Admin 仅能查看注销状态和必要审计信息，不能恢复已经完成匿名化的数据。

## 4. 韩国区域、定位与地点

### 4.1 区域主数据（P0，缺失）

#### 数据规则

- 从韩国官方行政区域数据同步，不以翻译后的名称作为关联键。
- 至少保存三级数据以支持 GPS 映射；用户筛选首发只展示两级。
- Level 1：特别市、广域市、特别自治市、道、特别自治道。
- Level 2：市、郡、区。
- Level 3：邑、面、洞（仅用于映射，不必全部展示）。
- 保存 `code`、`parent_code`、层级、韩/中/英名称、启用/废止日期。
- 行政区调整时停用旧记录，不改写历史活动。

#### API

```text
GET /regions?level=1
GET /regions?parentCode={code}
GET /regions/{code}
POST /admin/regions/sync
```

#### Admin

- 查看同步时间、来源版本、失败记录、未匹配外部代码和缺失翻译。
- 允许修正翻译、排序和热门区域，不允许随意修改官方代码。

### 4.2 自动定位（P0，需整改）

#### App Flow

```text
首页打开
→ 说明用途并请求前台定位权限
→ 手机系统返回经纬度
→ 调用 DAMUMU location/resolve
→ API 将坐标解析为标准 region_code
→ 首页显示“首尔 · 麻浦区”
→ 用户可刷新或改为手选区域
```

#### API

- 后端调用韩国适用的坐标转区域服务，优先使用法定区域代码映射。
- 外部代码通过 `region_provider_mapping` 关联 `region.code`。
- 沿 `parent_code` 得到城市和区；不能依赖中文/韩文名称匹配。
- 缓存坐标网格到区域的结果；外部密钥仅保存在服务端。
- 匹配失败时记录运营告警，并返回可手选状态。

#### 当前整改

- 当前 App 直接调用 Nominatim，只缓存经纬度和中文地名，尚未关联数据库区域。
- 首发改为 App 只取坐标，区域解析由 DAMUMU API 完成。

### 4.3 手动选择浏览区域（P0，缺失）

#### App Flow

```text
点击首页区域
→ 选择“当前位置”或“手动选择”
→ 选择市/道
→ 选择区/市/郡或“全部”
→ 保存 selected_region
→ 活动列表重新查询
```

- `current_location` 与 `selected_region` 分开保存。
- 用户人在首尔时也能浏览釜山活动。
- 拒绝定位权限不会阻止手动浏览和发布。

### 4.4 发布活动详细地点（P0，需整改）

#### App Flow

```text
发布活动 → 选择公开活动区域
→ 搜索车站、店铺或地址
→ 选择地点
→ 填写集合补充说明
→ 预览“公开信息”和“获批后信息”
```

#### 信息分级

公开：

- 城市和区。
- 模糊区域说明，如“弘大附近”。
- 可选的模糊距离。

私密：

- 具体地点名称。
- 完整地址。
- 精确经纬度。
- 集合说明和导航链接。

#### API 权限

- 活动列表和公开详情不得返回私密地点或精确坐标。
- 单独提供 `GET /events/{id}/meeting-place`。
- 仅组织者以及 `approved/attended` 成员可访问。
- `applied/waitlisted/rejected/withdrawn` 和游客不可访问。
- 权限由 API 判断，不能只靠 App 隐藏。

#### Admin

- 治理人员在具备对应权限和处置理由时查看详细地点，并写审计日志。
- 普通运营列表不默认展示精确地点。

## 5. 活动发现、搜索与筛选

### 5.1 首页与活动列表（P0，部分）

#### App Flow

```text
进入首页
→ 加载 selected_region 或当前位置
→ 加载默认筛选和排序
→ API 返回活动卡片
→ 搜索/更改筛选
→ 刷新列表
→ 打开活动详情
```

活动卡片首发字段：

- 标题、封面、分类、活动属性。
- 日期时间、公开区域、距离（有定位时）。
- 费用、已批准人数/容量、剩余名额状态。
- 组织者昵称、聚合评分和有效评价数。
- 不显示具体集合地点、参与者身份明细或联系方式。

### 5.2 可扩展筛选模型（P0，需整改）

当前四个写死 Chip 改为统一 `EventFilter`：

```text
regionCode / districtCode
from / to
categoryIds
featureTagCodes
languages
radiusMeters
priceMin / priceMax
hasAvailableSeats
minOrganizerScore
minOrganizerReviewCount
sort
query
```

#### UI

第一层快捷入口：

```text
[区域] [日期] [分类] [更多]
```

已选条件显示为可移除 Chip：

```text
[麻浦区 ×] [本周末 ×] [运动 ×] [新手友好 ×]
```

“更多”包含费用、距离、语言、剩余名额、活动属性、组织者评分和排序。不同筛选类型之间为 `AND`，同一多选类型内部默认 `OR`，页面必须明确表达。

#### API

- 服务端组合查询、白名单排序、分页和稳定游标/offset。
- 对常用条件建立索引；PostGIS 处理附近和距离排序。
- 评分筛选同时设置最低评价数，避免 1 条五星评价被当作高信誉。
- 返回 `applied_filters`，便于 App 与服务端规则一致。

#### Admin

- 管理可用快捷筛选、活动属性标签、排序和功能开关。
- 不能配置任意 SQL 或无法被 App 理解的表达式。

### 5.3 标签与派生条件（P0/P1，部分）

- 组织者标签：新手友好、室内、提供装备等，来自固定目录。
- 系统派生：今天、周末、免费、有名额、附近、高评分，不由组织者勾选。
- 热门/推荐：P2；首发使用可解释的时间、距离和基础热度排序。
- 不允许自由输入近义标签污染筛选目录。

### 5.4 参与者构成（P1，缺失）

- 性别资料自愿提供，并单独征得用于匿名统计的同意。
- 不推断未填写用户的性别。
- 只统计已批准且同意统计的用户。
- 低于隐私阈值（建议 5 人）显示“样本不足”，不显示精确人数。
- 显示粗粒度：较均衡、男性较多、女性较多、信息不足。
- 首发不做强制男女配额；如以后增加专场，作为明确参与资格独立设计。

## 6. 创建、编辑与发布活动

### 6.1 创建草稿（P0，部分）

#### App Flow

```text
点击发布
→ 选择可发布的小分类
→ 标题、描述、语言、活动属性
→ 日期和开始/结束时间
→ 人数、费用、参与要求
→ 公开区域和私密集合地点
→ 审核方式
→ 预览
→ 保存草稿或发布
```

#### API 校验

- 分类有效且为可发布层级。
- 标题、描述长度和敏感内容基础校验。
- `ends_at > starts_at`，时区统一 `Asia/Seoul` 存储为 `timestamptz`。
- 最少人数 ≤ 容量；费用范围有效；币种首发 KRW。
- `city_code/district_code` 必须来自启用区域主数据。
- 地点坐标解析出的区域与所选公开区域不一致时要求确认或拒绝。
- 属性标签必须来自启用目录。
- 幂等创建，防止网络重试产生重复活动。

#### Admin

- 查看活动、组织者、状态、公开信息和风险标记。
- 隐藏、恢复、取消活动；理由必填并通知组织者。
- 是否启用发布前审核由 Feature Flag 决定。

### 6.2 发布与时间限制（P0，缺失）

#### 发布检查

- 组织者账号正常且资料达到发布要求。
- 不存在该用户已经组织或已批准参与的重叠活动。
- 未超过组织活动周上限。
- 活动必填信息完整，集合地点有效。

冲突条件：

```text
existing.starts_at < new.ends_at
AND existing.ends_at > new.starts_at
```

- 草稿可以与其他草稿重叠；发布时必须检查。
- 检查在 API 事务中执行，不能只在 App 判断。
- 周上限通过运营配置管理，按 `Asia/Seoul` 周一至周日计算。

### 6.3 活动状态机（P0，部分）

```text
draft
→ pending_review（仅启用平台审核时）
→ published
→ full
→ completed

draft/pending_review/published/full
→ cancelled

published/full
→ hidden（Admin 治理）
→ published（Admin 恢复）
```

- 结束时间到达后由后台任务结算 `completed`，不能依赖用户打开 App。
- 取消后通知所有申请中、候补和已批准成员。
- 已开始或已有成员的活动修改时间、地点、容量时需要更严格规则和通知。

## 7. 报名、审批、名额与限制

### 7.1 报名（P0，部分）

#### App Flow

```text
活动详情 → 报名
→ 选择报名人数（首发可限制本人 1 人）
→ 填写申请说明
→ 选择是否向组织者共享联系方式
→ 确认公开区域和时间
→ 提交
→ 显示申请中/已批准/候补状态
```

#### API

- 用户不能报名自己的活动。
- 活动必须可报名、未开始、账号状态正常。
- 同一用户同一活动只有一条成员记录。
- 检查容量、周次数、黑名单和时间冲突。
- 手工审核模式创建 `applied`；自动模式在事务内决定 `approved/waitlisted`。
- 不能信任客户端提交的剩余名额或用户 ID。

### 7.2 申请状态机（P0，部分）

```text
applied → approved → attended
       ↘ rejected
       ↘ withdrawn

waitlisted → approved → attended
          ↘ withdrawn

approved → withdrawn / no_show
```

- 每次状态变化写入历史记录，不只覆盖当前值。
- 已处理申请不能被重复处理。
- 组织者不能看到申请人未授权的联系方式。

### 7.3 组织者审核（P0，部分）

#### App Flow

```text
我的组织活动 → 申请管理
→ 查看申请人公开资料、申请说明、聚合信用
→ 批准或拒绝
→ 拒绝必须填写理由
→ 申请人收到站内通知
```

#### API 原子检查

批准时重新检查：

- 活动仍可接受成员。
- 容量足够。
- 申请人没有新产生的重叠活动。
- 申请人没有达到周上限。
- 双方未互相屏蔽，账号均正常。

成功后同一事务内：

```text
更新 member 状态
→ 更新 approved_count
→ 必要时活动变 full
→ 开放 meeting-place 权限
→ 写站内通知
→ 写状态历史
```

### 7.4 时间冲突与周次数（P0/P1，缺失）

硬限制（P0）：

- 同一用户不能同时组织两个重叠活动。
- 不能同时批准参与两个重叠活动。
- 组织与参与之间也不能重叠。
- 组织者发布时检查；参与者自动批准/人工批准时检查。

周次数（P1，可配置）：

- 只计算组织的 `published/full/completed` 活动。
- 只计算参与的 `approved/attended/no_show` 活动。
- `applied/waitlisted/rejected/withdrawn/cancelled` 不占额度。
- 新用户与正常用户可使用不同上限，但规则必须公开、可解释。
- 达到上限时返回已使用次数、上限和下一次恢复时间。

## 8. 获批后的活动协作

### 8.1 私密集合地点（P0，缺失/需整改）

审批成功后 App 才请求私密地点接口。退出、被移除、活动取消或账号封禁后，API 立即撤销访问；App 不长期缓存精确地点，必要缓存需加密并设置到期时间。

### 8.2 活动会话与消息（P0/P1，部分）

#### App Flow

```text
获批
→ 加入活动会话
→ 查看历史消息
→ 发送文本消息
→ 标记已读
```

#### API

- 只有组织者和获批成员能进入活动会话。
- 退出/拒绝/封禁后禁止继续发送，是否保留只读历史由政策决定。
- 消息分页、长度限制、频率限制、敏感内容举报和删除状态。
- 首发可以轮询；系统推送和实时 WebSocket 可列为 P2，但站内通知必须存在。

#### Admin

- 不提供无条件浏览全部私聊的入口。
- 仅在举报/安全事件下按权限查看相关消息快照，并记录审计。

### 8.3 站内通知（P0，部分）

通知事件至少包括：

- 报名已提交。
- 报名批准/拒绝/候补转正。
- 活动时间或地点变更。
- 活动取消。
- 活动即将开始。
- 举报处理结果。
- 账号处置。

每条通知包含稳定类型、关联实体、创建时间、已读状态；点击后进入对应页面。敏感详细地点不得直接写进通知正文或系统推送预览。

### 8.4 签到与安全会面（P1，部分）

#### Flow

```text
活动开始窗口打开
→ 获批成员签到
→ 可启动安全会面
→ 定期确认安全
→ 用户结束会面或触发警报
→ Admin 安全队列响应
```

- 位置共享必须单独同意、限时、最小化保存并到期删除。
- 紧急联系人资料加密保存。
- 产品文案不能承诺替代警察、医疗或紧急服务。

## 9. 活动结束、评价与信用

### 9.1 活动完成（P0，缺失/部分）

- 后台任务在结束时间后把活动结算为 `completed`。
- 根据签到/组织者确认生成 `attended/no_show`，争议状态允许 Admin 处理。
- 只有共同参加过活动的用户之间可以评价。

### 9.2 评价（P1，部分）

#### App Flow

```text
活动完成 → 待评价
→ 选择共同参与用户
→ 1–5 星、固定评价标签、可选文字
→ 提交
→ 展示聚合评分
```

#### API

- 验证双方属于同一已完成活动且具有评价资格。
- 每个活动、评价人、被评价人唯一。
- 评分聚合考虑有效评价数；被治理删除的评价不计分。
- 防止用户评价自己、重复评价和未参加者评价。

#### 筛选交集

- 活动列表按组织者聚合评分筛选。
- 同时要求最低有效评价数，例如 `score >= 4.0 AND review_count >= 3`。
- 评分是系统派生值，不是活动标签。

#### Admin

- 查看被举报评价、隐藏违规评论、保留处置原因和评分重算记录。

## 10. 举报、屏蔽与治理

### 10.1 举报（P0，部分）

#### App Flow

```text
用户/活动/消息/评价菜单 → 举报
→ 选择固定原因
→ 补充说明和证据
→ 提交
→ 在“我的举报”查看状态
```

#### API

- 记录举报人、目标、类别、描述、证据快照和优先级。
- 防止重复滥用，保留必要证据但设置留存期限。
- 高风险类别自动提升优先级并进入安全队列。

#### Admin Flow

```text
submitted
→ triaged
→ investigating
→ resolved / dismissed
```

- 查看上下文、证据、相关历史和目标状态。
- 可联动隐藏活动、限制消息、暂停/封禁用户。
- 每次动作写治理记录和审计日志。
- 结案后向举报人发送不泄露他人隐私的结果通知。

### 10.2 屏蔽（P0，部分）

- 屏蔽后双方不能新建私聊，活动推荐和搜索尽量互相隐藏。
- 报名/审核时检查屏蔽关系。
- 已在同一活动中的安全处理需要明确：不能因屏蔽隐藏必要的活动变更，但应停止非必要直接沟通。
- 用户可在设置中查看并解除屏蔽。

## 11. Admin 首发模块

### 11.1 管理员认证与权限（P0，需整改）

角色建议：

- `support`：基础查询，不能看敏感地点。
- `moderator`：举报、用户和活动治理。
- `safety`：安全警报和必要位置权限。
- `catalog_manager`：分类、标签、区域翻译。
- `admin`：用户和配置管理。
- `owner`：管理员账户、关键安全配置。

所有敏感查看和写操作写入不可由普通管理员修改的审计日志。

### 11.2 运营概览（P1，已有）

- 用户、活动、报名、消息、举报、安全警报基础指标。
- 待处理队列必须可点击进入对应记录。
- 指标只用于运营，不作为资金或法律报表。

### 11.3 用户管理（P0，部分）

- 搜索、状态、公开资料、参与/组织记录、信用摘要、举报摘要。
- 暂停、恢复、封禁；原因必填。
- 敏感字段按角色脱敏。
- 支持账号注销状态和数据清理状态查询。

### 11.4 活动管理（P0，部分）

- 搜索、状态、区域、分类、组织者和举报数筛选。
- 查看公开详情、成员状态摘要和变更历史。
- 隐藏、恢复、取消活动并通知相关用户。
- 精确地点默认隐藏，仅授权角色按案件查看。

### 11.5 举报与安全警报（P0/P1，已有骨架）

- 按优先级、状态、目标类型分队列。
- 支持分派、调查、结案和误报。
- 联动用户/活动处置，形成完整审计链。
- 安全警报应有响应时限、联系人结果和结案说明。

### 11.6 目录、区域和标签（P0，部分/缺失）

- 分类三级维护及中英韩翻译。
- 兴趣和活动属性标签维护。
- 区域官方数据同步、映射异常和翻译校对。
- 启停而非删除已被引用的记录。
- 查看每项被活动使用的数量。

### 11.7 系统头像（P0，已有）

- 上传、校验、排序、中英韩名称、启停。
- 生产环境使用对象存储，不依赖容器临时磁盘。

### 11.8 Feature Flags 与运营规则（P0/P1，部分）

建议配置：

```text
event_pre_review_enabled
organizer_weekly_limit_new
organizer_weekly_limit_normal
participant_weekly_limit_new
participant_weekly_limit_normal
participant_composition_enabled
participant_composition_min_sample
safety_session_enabled
```

- 配置必须有类型、范围、默认值和修改审计。
- 安全硬规则不能通过普通 Feature Flag 完全关闭。

### 11.9 公告与审计（P1，已有骨架）

- 发布站内公告，支持生效时间、目标语言和停用。
- 审计记录操作人、动作、目标、前后状态摘要、原因、时间和来源 IP/会话。

## 12. 核心交叉规则

| 触发动作 | 同时影响的模块 |
|---|---|
| 用户切换语言 | App 文案、区域名称、分类/标签、缓存失效 |
| GPS 定位成功 | 区域映射、附近排序、首页显示；不改变手选区域，除非用户确认 |
| 活动发布 | 时间冲突、周上限、分类、标签、区域、地点、通知、Admin 可见性 |
| 报名批准 | 容量、时间冲突、周上限、地点权限、活动会话、通知、满员状态 |
| 活动地点改变 | 私密地点版本、已批准成员通知、必要时重新确认参与 |
| 活动取消 | 所有成员状态、通知、会话状态、周额度释放、退款预留 |
| 用户被封禁 | Token、发布/报名、消息、公开资料、未来活动和 Admin 审计 |
| 用户屏蔽另一用户 | 搜索推荐、报名资格、私聊、既有活动安全通信 |
| 举报成立 | 用户/活动/消息/评价处置、信用重算、通知、审计 |
| 活动完成 | 签到结算、评价资格、信用统计、历史活动 |

## 13. API 错误与并发规范

所有业务错误返回稳定错误码，例如：

```text
event_time_conflict
weekly_limit_reached
event_full
application_already_processed
meeting_place_forbidden
region_not_supported
account_suspended
version_conflict
```

App 根据错误码显示本地化说明和下一步操作。关键写操作使用事务、行锁/合适隔离级别、幂等键和 `row_version`，覆盖：

- 活动创建与发布。
- 报名和批准。
- 容量与候补。
- 取消活动。
- 举报处置。
- Admin 并发操作。

## 14. 生产与发布必备（P0）

- API、PostgreSQL/PostGIS、对象存储、HTTPS 域名和数据库备份。
- 密钥通过环境变量/密钥管理，不进入仓库和客户端。
- 上传文件病毒/格式/体积校验，公开与私密资源分开授权。
- 日志不记录密码、Token、完整联系方式和精确位置。
- 隐私政策、服务条款、社区规范、数据删除说明、支持联系方式网页。
- iOS/Android 权限文案与实际用途一致。
- Apple/Google 审核账号能体验完整报名审批闭环；审核期间后端在线。
- Release 禁止自动登录、开发 API Key、HTTP 明文地址和调试入口。
- 监控健康检查、错误率、延迟、磁盘、数据库连接和备份恢复。

## 15. 明确不进入首发

- 机器学习推荐和复杂个性化排序。
- 用户内容全量自动翻译。
- 强制男女比例配额。
- 支付、退款、平台抽成。
- 复杂等级会员体系。
- 全功能实时推送；首发可先保证站内通知。
- 精确到所有洞的用户筛选 UI；数据库可保留洞级映射数据。

## 16. 当前代码覆盖与关键缺口

| 功能 | 当前状态 | 首发动作 |
|---|---|---|
| 邮箱注册/登录 | 部分 | 正式 Token、限流、Release 关闭开发登录 |
| 资料/头像/兴趣 | 部分 | 隐私裁剪、对象存储、完整多语言 |
| 多语言 | 部分 | 消除硬编码、目录本地化、语言缓存 |
| GPS | 已有坐标获取 | 改为后端区域解析并关联 `region.code` |
| 韩国区域主数据 | 缺失 | 官方数据导入、翻译、同步和 Admin 页面 |
| 活动列表 API | 部分 | 统一筛选模型、分页、私密地点脱敏 |
| 四个首页筛选 Chip | 需整改 | 区域/日期/分类/更多的扩展式筛选 |
| 活动创建 | 部分 | 地点分级、区域校验、幂等和冲突限制 |
| 详细地点保护 | 需整改 | 公共接口移除地点/坐标，增加授权接口 |
| 报名与组织者审批 | 部分 | 时间冲突、周限制、状态历史和地点授权 |
| 通知 | 部分 | 覆盖状态变化、深链和敏感信息保护 |
| 消息 | 部分 | 活动成员权限、封禁/退出规则、分页限流 |
| 评价 | API 部分 | App Flow、聚合重算、Admin 治理、评分筛选 |
| 举报/屏蔽 | API/Admin 部分 | App 完整入口、处置反馈、跨模块联动 |
| 安全会面 | 部分 | 同意、数据生命周期、响应流程和真机测试 |
| Admin | 已有骨架 | 正式登录/RBAC、区域标签、敏感权限和审计 |
| 账号注销 | API 部分 | App 二次确认、活动处理、匿名化策略 |
| 生产部署 | 缺失/未确认 | HTTPS、数据库、对象存储、备份、监控 |

## 17. 建议实施顺序

### Phase A：先封住发布风险

1. 正式认证与 Admin 登录/RBAC。
2. 详细地点公开泄露整改。
3. 生产对象存储、HTTPS、密钥和备份。
4. Release 关闭开发登录和开发密钥。

### Phase B：完成区域与发现

1. 区域表、官方数据导入、外部代码映射。
2. GPS → API → `region.code`。
3. 手选区域。
4. 统一活动筛选模型和 App 筛选面板。

### Phase C：完成活动闭环

1. 发布表单和私密地点。
2. 时间冲突和周次数规则。
3. 报名、审批、容量、候补和通知事务。
4. 获批地点、活动会话、取消与变更通知。

### Phase D：治理与收尾

1. 活动完成、签到、评价和信用聚合。
2. 举报、屏蔽、安全警报跨端闭环。
3. 账号注销和数据生命周期。
4. 多语言清扫、无障碍、真机测试和商店材料。

## 18. 首发验收场景

至少使用组织者 A、申请人 B、申请人 C、管理员 M 完成：

1. A 注册、完善资料并发布麻浦区活动。
2. 未登录用户能看到活动，但看不到具体地点和坐标。
3. B 用 GPS 自动定位麻浦区，也能手动切换到江南区再切回。
4. B 使用区域、日期、分类和新手友好组合筛选找到活动。
5. B 报名后仍看不到具体地点。
6. A 审批 B；API 再次检查容量、冲突和周限额。
7. B 收到通知，随后能访问具体地点和活动会话。
8. C 未获批，抓取公开接口也无法获得具体地点。
9. B 尝试报名重叠活动，被明确阻止。
10. A 变更地点或取消活动，B 收到不泄露敏感信息的通知。
11. 活动结束后 B 可评价 A；未参加用户不能评价。
12. B 举报活动或消息，M 能分诊、处置并产生审计记录。
13. M 封禁用户后，其现有 Token 无法继续发布、报名或发消息。
14. 用户完成账号注销，App 清除会话，公开资料停止展示。
15. 中文、韩文、英文分别完成上述核心路径，无阻断性硬编码文案。

以上场景全部通过，才具备提交首次公开审核的业务条件。
